# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: Vorlesungsskript Mathematik I
author: Thomas Carraro  
logo: logo.png

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: force

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: Mathematik_I.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/github-mathe/Mathematik_I  # Online location of your book
  path_to_book: docs  # Optional path to your book, relative to the repository root
  branch: main  # Which branch of the repository should be used when creating links (optional)

html:
  baseurl: ""  # Set this to the repository name
  theme:
    name: sphinx_book_theme
    options:
      navigation_depth: 2  # Sidebar navigation levels
      show_navbar_depth: 2  # Levels of headers shown in sidebar
      collapse_navigation: False  # Keep the navigation expanded


